import {StyleSheet} from "react-native";
import { colors } from "./colors.ts";

const styles = StyleSheet.create({
  text: {
    fontSize: 14,
    color: colors.black,
    textAlign: 'center',
  }
});

export { styles };
